SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.active_empnl_hosp_specialities_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_empnl_nabh_details_mv;  

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_empnl_duty_doctors_hosp_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.empnl_hosp_medco_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_empnl_paramedics_doctors_hosp_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_empnl_specialist_type_hosp_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_active_mithra_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_pending_claims_dtls;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_igrt_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.pending_claims;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.PMJAY_cases;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_hosp_daily_feedback_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.PEX_verified_preauth_approve_time;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.preauth_approve_discharge_time;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.spec_procedure_claims_count;


SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.delivery_case_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.new_govt_panel_doctor_performance;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_ppd_cpd_performance_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.govt_hosp_spec_preauths_rs;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.hosp_doctor_splty;


SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.hosp_empanelment; 

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.medico_forwarded_nam_forwarded_time;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.medco_to_nam_forwarded_delay_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_nam_to_preauth_forwarding_delay_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_onbed_pat_visit_fdbk_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.empnl_hosp_active_doctors_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.hospdist_vs_patientdist_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.foss_aasra_people_benefit_overview_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_aasra_paid_overlap_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.speciality_inclusion;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.trust_doctor_performance_daily;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.ration_card_procedure_multiple_times_utilization;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.family_multiProc_case_data;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.ration_card_speciality_multiple_times_utilization;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.PMJAY_cards;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.government_hosp_deliveries;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.cmco_preauth_approved_cases;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.spec_procedure_preauths_count;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.ration_card_pat_proc_utilization_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_followup_case_claim_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.foss_dist_wise_anm_feedback_report_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.asri_mithra_service_fdbk_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.asri_flagged_cases_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_chemo_days_diff_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.ration_card_pat_spec_utilization_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.asri_discharge_facilitation_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.asri_grievances_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.anm_feedback_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.rep_hosp_dist_rejections_mv;  -- not there

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.cancer_data_analysis;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.case_regn_preauth_approve_time;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.case_regn_medico_forwarded_time;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.cancer_data_with_procedures_analysis;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_ip_op_registration_mv_1;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.foss_asri_people_benefit_overview_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_dist_wise_proc_total_amount_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from  dwh.asri_case_wise_preauth_apprv_aasra_paid_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_onbed_check_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.followup_stages_tracking;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.case_wise_aasra_eligible_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.asri_dist_wise_proc_total_cases_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh.wt_case_details_mv;
