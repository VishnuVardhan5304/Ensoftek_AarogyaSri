SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.op_exec_perf_rep_mv ;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_preauth_pending_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_claims_pending_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_active_hospitals_dtls_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.MR_Online;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.MR_Offline;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ppd_perf_rep_mv ;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ptd_perf_rep_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.cpd_perf_rep_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ctd_perf_rep_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ftd_perf_rep_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_preauth_pending_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_claims_pending_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_arsi_common_emp_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_followup_pending_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_chronic_op_claims_pending_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.chronic_op_report_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.bifurcation_emp_pen_case_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_followup_pending_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_followup_case_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_unique_patient_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.mis_followup_availed_case_details;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_followup_tracking_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_followup_case_claim_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_empanelled_hosp_list_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.MR_AUDIT_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.wt_case_preauth_claim_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_emp_pen_enroll_details_mv;

SELECT max(last_refreshed_dt) as latest_refreshed_date from dwh_ehs.ehs_jrnlst_enroll_details_mv;
